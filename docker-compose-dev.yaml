services:
  vprouter:
    build: .
    env_file:
      - .env
    sysctls:
      - net.ipv4.ip_forward=1
    devices:
      - /dev/net/tun
    cap_add:
      - NET_ADMIN
    volumes:
      - /tailscale:/var/lib/tailscale # Tailscale state directory
